# Core dependencies
llama-cpp-python>=0.3.0
huggingface-hub>=0.20.0
psutil>=5.9.0

# PyTorch - IMPORTANTE: Instalar con CUDA para aceleración GPU
# Por defecto, pip instala PyTorch CPU-only. Para CUDA:
# pip uninstall torch torchvision torchaudio
# pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0
